<% @jobs.each do |job| %>
  <h1> <%= job.title %> </h1>
  <%= job.company.name %>
  <%= job.location %>
  <% if job.featured? %>
    <strong> Destaque </strong>
  <% end %>
  <%= link_to "#{job.title}" , job %>
<% end %>
